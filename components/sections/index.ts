// Sections barrel export
// Add page-specific section components here as they are created
// e.g. export { default as HeroSection } from './HeroSection';
// e.g. export { default as ServicesSection } from './ServicesSection';
// e.g. export { default as ProjectsGrid } from './ProjectsGrid';
